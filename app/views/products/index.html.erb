<h1>Products :O</h1>

<ul>
  <% @products.each do |product| %>
    <li><%= link_to product.title, product_path(product) %></li>
  <% end %>
</ul>

<h2><%= link_to '+ Start selling', new_product_path %>
